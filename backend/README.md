# Backend (Spring Boot)

```bash
cd backend
mvn spring-boot:run
```

API: http://localhost:8080  
Swagger: http://localhost:8080/swagger-ui.html

Requires PostgreSQL (`training_platform` / `training_user` / `training_password`).  
Use root `docker compose up -d postgres` or the full stack compose.
